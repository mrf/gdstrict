# Rust
/target/
**/*.rs.bk
Cargo.lock.orig

# gdstrict scratch
/fixtures/**/*.tmp.gd

# Godot (test fixtures / sample projects)
.godot/
*.import
export_presets.cfg
# gdstrict injects override.cfg during strict checks; never commit a leaked copy.
override.cfg

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
.orchestrator-*

# Local agent/orchestrator state — never commit
.claude/worktrees/
.claude/scheduled_tasks.lock
