[workspace]
resolver = "2"
members = [
    "crates/gdstrict-syntax",
    "crates/gdstrict-strict",
    "crates/gdstrict-format",
    "crates/gdstrict-cli",
    "crates/gdstrict-lint",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/mrf/gdstrict"
description = "Fast GDScript formatter and strict-typing enforcement layer"

# Config for 'dist'
[workspace.metadata.dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-musl", "x86_64-apple-darwin", "x86_64-unknown-linux-musl", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Whether to install an updater program
install-updater = false

[workspace.dependencies]
tree-sitter = "0.24"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
